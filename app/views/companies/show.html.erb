<%= render 'shared/header' %>

<div class='ui main container'>
  <h1 class="ui header">会社別ゲーム一覧</h1>
  <div class="ui divider"></div>

  <div class="ui two column grid">
    <div class="four wide column">
    
      <%= render 'shared/category_menu', { categories: @companies, category: @company, link_path: "company_path" } %>

    </div>
    <div class="twelve wide column">

      <%= render 'shared/game_list', { games: @games, q: @q, menu_title: @company.name } %>

    </div>
  </div>
</div>