<% current_user.taggings.includes(:tag).where(game_id: game).each do |tagging| %>
  <%= link_to tagging.tag.name, game_tagging_path(game, tagging), method: :delete, class: 'ui small tag label', remote: true %>
<% end %>